server:
  port: 8484

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
      instance:
        prefer-ip-address true

spring:
  application:
    name: API-GATEWAY
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true



